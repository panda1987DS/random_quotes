services:
  - type: web
    name: random-quotes
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn random_quotes.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: random_quotes.settings